summary: provide an easy to use interface to configure different authentication backends
require:
  - library(distribution/opts)
  - library(distribution/Log)
  - library(distribution/tcf)
  - library(pam)
